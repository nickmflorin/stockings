/* eslint-disable */
import { z } from 'zod';

export const PriceChangeEventConfigScalarFieldEnumSchema = z.enum(["id", "createdAt", "createdById", "updatedAt", "updatedById", "productNotificationConfigId", "contentType", "eventTypes"])
