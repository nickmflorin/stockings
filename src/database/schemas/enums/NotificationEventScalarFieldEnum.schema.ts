/* eslint-disable */
import { z } from 'zod';

export const NotificationEventScalarFieldEnumSchema = z.enum(["id", "createdAt", "createdById", "updatedAt", "updatedById", "productId", "userId", "contentType"])
