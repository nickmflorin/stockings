/* eslint-disable */
import { z } from 'zod';

export const ProductRecordScalarFieldEnumSchema = z.enum(["id", "createdAt", "createdById", "updatedAt", "updatedById", "timestamp", "productId", "price", "rawPrice", "status", "wasManuallyCreated", "manuallyChangedFields"])
