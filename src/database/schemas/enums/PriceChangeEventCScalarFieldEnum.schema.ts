/* eslint-disable */
import { z } from 'zod';

export const PriceChangeEventCScalarFieldEnumSchema = z.enum(["id", "createdAt", "createdById", "updatedAt", "updatedById", "productId", "userId", "contentType", "eventTypes"])
