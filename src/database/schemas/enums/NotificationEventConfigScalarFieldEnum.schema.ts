/* eslint-disable */
import { z } from 'zod';

export const NotificationEventConfigScalarFieldEnumSchema = z.enum(["id", "createdAt", "createdById", "updatedAt", "updatedById", "productNotificationConfigId", "contentType"])
