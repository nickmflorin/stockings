/* eslint-disable */
import { z } from 'zod';

export const StatusChangeEventConfigScalarFieldEnumSchema = z.enum(["id", "createdAt", "createdById", "updatedAt", "updatedById", "productNotificationConfigId", "contentType", "eventTypes"])
