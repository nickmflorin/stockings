/* eslint-disable */
import { z } from 'zod';

export const UserScalarFieldEnumSchema = z.enum(["id", "clerkId", "firstName", "lastName", "profileImageUrl", "emailAddress", "createdAt", "updatedAt"])
