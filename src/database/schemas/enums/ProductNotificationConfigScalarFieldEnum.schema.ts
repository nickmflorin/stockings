/* eslint-disable */
import { z } from 'zod';

export const ProductNotificationConfigScalarFieldEnumSchema = z.enum(["id", "createdAt", "createdById", "updatedAt", "updatedById", "productId", "userId"])
