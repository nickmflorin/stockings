/* eslint-disable */
import { z } from 'zod';

export const StatusChangeEventScalarFieldEnumSchema = z.enum(["id", "createdAt", "createdById", "updatedAt", "updatedById", "productId", "userId", "contentType", "eventTypes"])
